# CLI

Minimal ACME certificate management command-line interface. Provides subcommands
for certificate issuance, renewal, revocation, and account management using
`SyncClient` and `FileStore`.

::: lacme.cli.main
    options:
      show_root_heading: true
